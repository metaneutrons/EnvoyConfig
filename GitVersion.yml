mode: ContinuousDeployment  # Good for automatic versioning on each commit

assembly-versioning-scheme: MajorMinorPatch
assembly-file-versioning-scheme: MajorMinorPatch
assembly-informational-format: '{InformationalVersion}'

tag-prefix: '[vV]'  # Tags like v1.0.0 will be recognized

branches:
  main:
    regex: ^main$
    increment: Patch
    is-release-branch: true
    label: ''  # No prerelease label on main → e.g., 1.2.3

  develop:
    regex: ^develop$
    increment: Minor
    is-release-branch: false
    label: alpha  # This enables prerelease versions like 0.2.0-alpha.5
    track-merge-target: false

ignore:
  sha: []
